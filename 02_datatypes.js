"use strict"; // treat all js code as newer version

// alert(3+3) we are using nodejs.

// ; is not important in js, but it is a good practice to use it.
console.log(3 + 3)
console.log("Prince")

let name = "prince"
let age = 19;
let islogggedIn = false;

// number => 2 to power 53;
//bitint => used for large numbers
// string => "prince"
// boolean => true/false
// null is standalone value, it is not a type.
// null and undefined is different.


console.log(typeof "Prince"); // string
console.log(typeof null); // object (this is a bug in js, null is not an object)
console.log(typeof undefined); // undefined